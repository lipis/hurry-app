$ ->

$ -> ($ 'html.countdown').each ->
  init_countdown()

$ -> ($ 'html.profile').each ->
  init_profile()

$ -> ($ 'html.feedback').each ->
  init_loading_button()

$ -> ($ 'html.user-list').each ->
  init_user_list()

$ -> ($ 'html.admin-config').each ->
  init_admin_config()

